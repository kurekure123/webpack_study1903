// 기본 함수형
// var area = function () {
//   return Math.PI * r * r;
// }
//
// var circumference  = function () {
//   return 2 * Math.PI * r;
// }

//named export

export var area = function (r) {
  return Math.PI * r * r;
};

export var circumference = function (r) {
  return 2 * Math.PI * r;
};

